// Package run will execute the generated workflow locally, with act inside
// Docker, and check that Docker is available beforehand.
//
// It is empty on purpose: the current increment stops at generating the
// workflow. Local execution is the next one, and the package is already here
// so that its place in the architecture is not in question.
package run
